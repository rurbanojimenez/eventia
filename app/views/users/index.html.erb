<p>List of registered users:</p>
<div class="table-responsive">
	
	<table class="table">
		<thead>
			<tr>
				<th><strong>User</strong></th>
				<th><strong>Email</strong></th>
			</tr>
		</thead>
		<tbody>
			<% @users.each do |eachuser| %>
				<tr>
					<td>
						<%if current_user == eachuser %>
							<%= link_to eachuser.name, eachuser %>
						<%else%>
							<%= eachuser.name %>
						<%end%>
						
					</td>
					<td>
						<%=eachuser.email%>
					</td>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>